<div class="features">
	<h2 class="features__title">About storgage</h2>
	<ul class="features__list">
		{{#each features}}
			<li class="features__item">
				{{>feature}}
			</li>
		{{/each}}
	</ul>
</div>